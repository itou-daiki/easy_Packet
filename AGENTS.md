# easyPacket — エージェント向け作業規約

このリポジトリで作業するすべてのAIエージェント（Antigravity 等）は、着手前に本ファイルと
`docs/00_調査レポート.md` `docs/01_実装計画_Antigravity.md` を必ず読むこと。

## このプロジェクトは何か

山梨大学「情報ネットワーク演習」で使う**授業用の学習教材**です。一般的なWebアプリではありません。
したがって次の優先順位で判断すること。

1. **授業が止まらないこと** — 学内ネットワーク・学内プロキシ・オフラインでも必ず動く
2. **教える内容が正しいこと** — 生徒が覚えてしまう情報に誤りがあってはならない
3. **誰でも使えること** — 端末・画面サイズ・支援技術を問わない
4. 見た目の洗練・コードの美しさ

## 絶対に守ること

- **外部APIに依存する実装を新たに足さない。** 授業中に外部サービスが落ちる／塞がれる／レート制限に
  当たるリスクを常にゼロにする。オンライン機能を足す場合は必ず「既定オフライン＋任意でライブ」の
  二層にし、ライブが失敗しても授業が続くようにする。
- **ダミーのIPアドレスは必ず予約領域から選ぶ。** RFC 5737（`192.0.2.0/24`, `198.51.100.0/24`,
  `203.0.113.0/24`）、RFC 1918（`10/8`, `172.16/12`, `192.168/16`）、RFC 6598（`100.64/10`）。
  実在組織に割り当て済みのグローバルIPを「架空のルータ」として使わない。ランダム生成もしない。
- **`innerHTML` を使わない。** 文字列からDOMを組み立てない。`textContent` と
  `document.createElement` を使う。
- **`canvas.width` / `canvas.height` を、その canvas 自身の測定値から算出しない。**
  必ず親要素を測る。canvas の CSS 幅は CSS 側で固定する。（F-1 の再発防止）
- **非同期処理の状態フラグは必ず `try/finally` で戻す。**（F-13 の再発防止）
- 破壊的な git 操作（`push --force`, `reset --hard`, ブランチ削除）を勝手に行わない。
- 既存の日本語UIテキストの語調を変えない（です・ます調、絵文字付き見出し）。

## 作業の進め方

1. 着手前に**実行計画の概要を提示して承認を得る**。いきなり編集を始めない。
2. 1フェーズ＝1コミット。フェーズをまたいで混ぜない。
3. 各フェーズの完了条件（`docs/01_実装計画_Antigravity.md` の「検証」）を**実測で**満たしてから次へ進む。
   「直したはず」ではなく、数値かスクリーンショットを出すこと。
4. 完了報告には必ず**実行前後のスクリーンショット**と、検証コマンドの出力を添える。

## 動作確認のしかた

```bash
cd <リポジトリルート>
python3 -m http.server 8777
# ブラウザで http://127.0.0.1:8777/index.html を開く
```

`file://` で直接開いた場合も動くことを確認すること（`fetch()` がCORSで失敗する環境）。

## コミットメッセージ

```
<type>: <説明>
```
type は `feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `perf` / `ci`。日本語で書いてよい。
